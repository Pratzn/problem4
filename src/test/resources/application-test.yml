spring:
  application:
    name: problem4
  datasource:
    driverClassName: org.h2.Driver
    password: password
    url: jdbc:h2:mem:test
    username: sa
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true
logging:
  level:
    root: info